import React from 'react'

interface RestaurantPostPageProps {
    id: string
}

export const RestaurantPostPage = (props: RestaurantPostPageProps) => {
    return (
        <div>RestaurantPostPage</div>
    )
}
